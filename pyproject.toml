[project]
name = "harnesslocal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hamza El Rachdi", email = "hamzarachidi04@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.21.0",
    "deepagents>=0.7.6",
    "fastapi>=0.141.1",
    "mcp[cli]>=2.0.0",
    "python-dotenv>=1.0.0",
    "uvicorn>=0.32.0",
]

[project.scripts]
harnesslocal = "harnesslocal:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
